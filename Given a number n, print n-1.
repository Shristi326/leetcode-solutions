#include <iostream>

void printNumbers(int n) {
    if (n <= 0) {
        return;
    }
    else {
        std::cout << n << " ";
        printNumbers(n - 1);
    }
}

int main() {
    int n = 10;
    printNumbers(n);
    return 0;
}
