class Solution:
    def targetIndices(self, nums: List[int], target: int) -> List[int]:
        list1=sorted(nums)
        res=list()
        for i in range(len(list1)):
            if list1[i]==target:
                res.append(i)
        return res
